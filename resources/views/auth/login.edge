<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Connexion - Cartographie Services Kalya</title>
  
  <!-- Tailwind CSS + DaisyUI -->
  <link href="https://cdn.jsdelivr.net/npm/daisyui@4.4.24/dist/full.min.css" rel="stylesheet" type="text/css" />
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="min-h-screen bg-gradient-to-br from-primary to-secondary flex items-center justify-center">
  <div class="card w-96 bg-base-100 shadow-2xl">
    <div class="card-body text-center">
      <h1 class="card-title text-2xl justify-center mb-4">
        🗺️ Cartographie Services
      </h1>
      
      <p class="text-base-content/70 mb-6">
        Connectez-vous avec votre compte Kalya pour accéder à la cartographie des services.
      </p>
      
      @if(flashMessages.has('error'))
        <div class="alert alert-error mb-4">
          <span>{{ flashMessages.get('error') }}</span>
        </div>
      @endif
      
      @if(flashMessages.has('success'))
        <div class="alert alert-success mb-4">
          <span>{{ flashMessages.get('success') }}</span>
        </div>
      @endif
      
      <div class="card-actions justify-center">
        <a href="{{ route('auth.oauth.login') }}" class="btn btn-primary btn-wide">
          <svg class="w-5 h-5 mr-2" fill="currentColor" viewBox="0 0 20 20">
            <path fill-rule="evenodd" d="M3 4a1 1 0 011-1h12a1 1 0 011 1v2a1 1 0 01-1 1H4a1 1 0 01-1-1V4zM3 10a1 1 0 011-1h6a1 1 0 011 1v6a1 1 0 01-1 1H4a1 1 0 01-1-1v-6zM14 9a1 1 0 00-1 1v6a1 1 0 001 1h2a1 1 0 001-1v-6a1 1 0 00-1-1h-2z" clip-rule="evenodd"></path>
          </svg>
          Se connecter avec Kalya
        </a>
      </div>
      
      <div class="divider">ou</div>
      
      <p class="text-xs text-base-content/50">
        Vous n'avez pas accès ? Contactez l'administration Kalya.
      </p>
    </div>
  </div>
  
  <script>
    // Auto-redirection après 3 secondes si pas d'interaction
    let autoRedirectTimer = setTimeout(() => {
      window.location.href = "{{ route('auth.oauth.login') }}";
    }, 5000);
    
    // Annuler l'auto-redirection si interaction
    document.addEventListener('click', () => {
      clearTimeout(autoRedirectTimer);
    });
  </script>
</body>
</html>
